# Artifact D — HTML Wireframe with Populated Copy

Combine Artifact B copy quality with Artifact C HTML structure.

Requirements:
- Full HTML file per service with real copy (no placeholder body text).
- Include hero + contact form with GDPR consent.
- Include all 7 content blocks from `copy_blocks.md`.
- Include populated JSON-LD from `schema_templates.md`:
  - FAQPage (5-7 Q&A)
  - HowTo where relevant
- Apply vertical compliance rules from `vertical_compliance.md`.

SEO checks:
- Focus keyword in slug intent, H1, title, meta, intro, and one H2.
- Word count target suitable to page type (master/geo).
