# Artifact C — HTML Wireframe (No Copy)

Output production-ready HTML skeleton per service with placeholders only.

Requirements:
- No marketing copy; use `{{placeholders}}` for text fields.
- Include hero section with contact form fields:
  - name, email, phone, company, message, GDPR consent, submit
- Include 7 block containers matching `copy_blocks.md`.
- Include JSON-LD script tags with placeholder values using `schema_templates.md`.
- Keep semantic HTML structure ready for CMS import.

Output format:
- One `.html` file per service.
- Clean sections and predictable IDs/classes.
